# Requirements for Bible LLM Project
# Build a Large Language Model from Scratch

# Core dependencies
torch>=2.0.0          # PyTorch - deep learning framework
tiktoken>=0.5.0       # OpenAI's BPE tokenizer (GPT-2)

# Optional but recommended
numpy>=1.24.0         # Numerical computing
matplotlib>=3.7.0     # Plotting (for loss curves, etc.)
tqdm>=4.65.0          # Progress bars for training

# Note for Windows users:
# If you encounter DLL errors with PyTorch, you may need to install
# Visual C++ Redistributable from Microsoft:
# https://learn.microsoft.com/en-us/cpp/windows/latest-supported-vc-redist
